// Shared business rules for the trading platform.

export const MAX_PORTFOLIOS_PER_USER = 20;

// Guardrails for the user-chosen starting balance.
export const MIN_STARTING_BALANCE = 100;
export const MAX_STARTING_BALANCE = 100_000_000;
export const DEFAULT_STARTING_BALANCE = 100_000;
